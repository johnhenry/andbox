name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: johnhenry/workflows/.github/workflows/ci.yml@v1
    with:
      node-versions: '["24"]'
      os: '["ubuntu-latest"]'
      # andbox has no committed package-lock.json.
      install-command: npm install
      node-cache: ""
      gate-commands: |
        npm test
        npm run examples
